{
  wayland.windowManager.niri.settings.input = {
    keyboard = {
      xkb = {
        layout = "us";
        options = "ctrl:nocaps";
      };
      numlock = true;
    };

    touchpad = {
      tap = { };
      natural-scroll = { };
    };
  };
}
